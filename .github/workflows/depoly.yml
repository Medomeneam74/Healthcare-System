name: Deploy Smart Healthcare System

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Pull code and Deploy
        run: |
          cd ~/v1/nfc-demo0
          git pull origin main
          sudo docker compose build --no-cache frontend
          sudo docker compose up -d
